import { Box, Typography } from "@mui/material";

export default function CartPage() {
  return (
    <Box>
      <Typography variant="h6">
        Cart page (optional). Use the Cart Drawer for main cart UX.
      </Typography>
    </Box>
  );
}
